# ── macOS ──────────────────────────────────────────────────────────────────
.DS_Store
.DS_Store?
._*

# ── Internal / Claude files (not for client)
.claude/
A03MAQANE.md
brand_files/
comprehensive_report_*.xlsx

# ── Firebase local cache ────────────────────────────────────────────────────
.firebase/
.firebaserc.local

# ── Node (admin panel + Cloud Functions) ────────────────────────────────────
node_modules/
dist/
dist-ssr/
*.local
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# ── Flutter / Dart ──────────────────────────────────────────────────────────
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins-dependencies
.pub-cache/
.pub/
/herdiq/build/
/coverage/
app.*.symbols
app.*.map.json
*.iml
*.ipr
*.iws
.idea/

# Code generation
# *.g.dart files are committed intentionally — Isar requires them at build time
*.freezed.dart
*.mocks.dart

# ── Secrets (excluded from open-source repos; included here for private handover) ──
# google-services.json, GoogleService-Info.plist, key.properties, *.jks → committed
firebase_options.dart
.env
.env.*

# ── Build outputs ────────────────────────────────────────────────────────────
/herdiq/android/build/
/herdiq/android/app/debug/
/herdiq/android/app/profile/
/herdiq/android/app/release/
